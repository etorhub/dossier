# Optional merge for self-hosted Ollama: wait for model pull before worker starts.
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.local-llm.yml --profile local-llm up -d
services:
  worker:
    depends_on:
      db:
        condition: service_healthy
      ollama-init:
        condition: service_completed_successfully
